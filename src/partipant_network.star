

def launch_participant_network(plan, participants, network_params, global_log_level):
    # num participants

    # generate cl validator keystores

    # generate el genesis data

    # get the prefunded keys artifact

    # create el client launcher objects
    # launch el clients

    # grab genesis generation config templates
    # create cl genesis data
    # launch cl clients

    # return all participants and genesis timestamp